<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user.Username %>-Your Dashboard</title>
    <link rel="stylesheet" type="text/css" href="/Dashboard.css"/>
</head>
<body>
    <nav>
        <h1>DB-Association</h1>
        <div class="sign">
            <img src="/<%= user.UserImage %>"/>
            <a href=""><%= user.Username %></a>
        </div>
    </nav>
    <div class="main">
        <div class="btns">
            <h1>Menu</h1>
            <a>Total Post:</a>
            <a href="/newpost/<%= user._id%>">Add New Post</a>
            <a href="/">Go to Home</a>
            <a href="/logout">Logout</a>
            
        </div>
        <div class="posts">

            <% user.post.reverse().forEach((post)=>{ %>
                <div class="post">
                    <img class="post-image" src="/<%= post.PostImage %>"/>
                    <h2><%= post.Title %></h2>
                    <h3><%= post.Bio %></h3>
                    <div class="user">
                        <img src="/<%= user.UserImage %>"/>
                        <h4><%= user.Username %></h4>
                    </div>
                </div> 
                <% }); %>
           
            
        </div>
    </div>
</body>
</html>